export enum ResourceAPI {
  BaseResourceUrl = "/resource/file/",
  AllFolder = "/resource/folder",
  FolderUrl = "/resource/folder",
}

export const acceptFileTypes = [".doc", ".docx", ".pdf", ".zip", ".rar"];
